— Authentication & Authorization —
# Асуултын жагсаалт

**16.** Controller–Service–Repository гэсэн 3 давхаргын dependency flow-г тайлбарла. Controller яагаад Repository-г шууд дуудаж болохгүй вэ?

---

## DTO, Exception, Transaction, SOLID

**17.** DTO (Data Transfer Object) pattern яагаад хэрэгтэй вэ? Entity-г шууд буцаах ба DTO буцаахын ялгааг аюулгүй байдлын талаас нь тайлбарла.

**18.** Global Exception Handler (`@ControllerAdvice` эсвэл express-ийн error middleware) яагаад хэрэгтэй вэ? Алдааг Controller бүр дотор шийдэх ба нэг газар шийдэхийн ялгаа нь юу вэ?

**19.** Transaction-д ACID гэсэн 4 шинж чанар байдаг. Мөнгө шилжүүлэх жишээн дээр хасах болон нэмэх үйлдлийн дунд алдаа гарвал Transaction байхгүй ба байх тохиолдолд юу болохыг харьцуул.

**20.** SOLID-ийн S (Single Responsibility) болон D (Dependency Inversion) зарчмыг тайлбарла. Controller-д бизнесийн логик байх нь аль зарчмыг зөрчдөг вэ? Interface ашиглах нь аль зарчмыг хэрэгжүүлдэг вэ?






# Номын сангийн систем — API & Сервер Кодын Даалгаврууд
## VIII хэсэг: Нэгтгэсэн даалгаврууд

**31.** `authorController.js`-д зохиолч нэмэх `create` болон бүх зохиолчийн жагсаалтыг авах `getAll` функцийг бич.

---

**32.** `bookController.js`-д ном нэмэхдээ зохиолчийг мөн хамт холбодог `create` функцийг бич. Дараах дарааллаар хий:
1. Ном нэмнэ → `insertId` авна
2. `зохиолч_id`-уудыг `Ном_Зохиолч` хүснэгтэд холбоно

---

**33.** `loanController.js`-д тухайн гишүүний зээлдэлтийн бүх түүхийг буцаадаг `getByMember` функцийг бич. `req.params`-аас `id`-г авна.

---

**34.** `bookController.js`-д номын боломжтой хувийн тоог буцаадаг `getAvailable` функцийг бич.
```
GET /api/books/:id/available
```

---

**35.** `app.js`-д бүх middleware болон route-уудыг зөв дарааллаар бүртгэсэн бүтэн файлын кодыг бич. Дараалал:
1. `express.json()`
2. Route-ууд

---

**36.** Дараах бүтэцтэй `library-api` төслийн `package.json`-д хэрэгтэй бүх dependency болон script-уудыг бич:
- **Dependencies:** `express`, `mysql2`, `jsonwebtoken`, `dotenv`
- **DevDependencies:** `nodemon`
- **Scripts:** `start`, `dev`

---

## IX хэсэг: Нэмэлт даалгаврууд

**37.** `AuthorModel`-д дараах 3 функцийг бич:
- `getAll()` — бүх зохиолч
- `getById(id)` — ID-аар хайх, олдоогүй бол `null`
- `create({нэр, харьяалал})` — шинэ зохиолч нэмж `insertId` буцаана

---

**38.** `LoanModel`-д тухайн гишүүний зээлдэлтийн бүх түүхийг авах `getByMember(гишүүн_id)` функцийг бич. Номын гарчиг болон авсан, буцаасан огноог хамт буцаана.

---

**39.** `authorRoutes.js`-д дараах route-уудыг тодорхойлсон кодыг бич:

| Method | URL | Controller функц |
|--------|-----|-----------------|
| GET | `/` | `getAll` |
| GET | `/:id` | `getById` |
| POST | `/` | `create` |

---

**40.** `bookController.js`-д нэг номын дэлгэрэнгүй мэдээллийг буцаадаг `getById` функцийг бич. Номын гарчиг, ISBN, он, зохиолчдын жагсаалтыг хамт буцаана. `BookModel.getByIdWithAuthors(id)`-г дуудна.

---

**41.** `BookModel`-д `getByIdWithAuthors(id)` функцийг бич. Ном болон түүний зохиолчдын мэдээллийг `JOIN` ашиглан нэг query-д авч буцаана.

